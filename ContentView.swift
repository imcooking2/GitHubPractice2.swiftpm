import SwiftUI

struct ContentView: View {
    var body: some View {
        NavigationLink(destination: Text("NounView")) {
            
        }
        .navigationTitle("Navigation")
    
    }
}
