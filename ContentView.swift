import SwiftUI

struct ContentView: View {
    var body: some View {
        
      
        NavigationView {
            NavigationView {
                        VStack {
                            NavigationLink("Navigation", destination: Text("NounView"))
        }
    
    }
}
                                           }
                                           }
                                           
